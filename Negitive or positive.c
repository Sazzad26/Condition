#include<stdio.h>
int main()
{
        int n;
        printf("\n Enter the value of n");
        scanf("%d", &n);
    if(n>0)
        printf("\n The number is positive %d", n);
    else
        printf("\n The number is negative %d \n", n);
    return 0;
}
